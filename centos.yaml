apiVersion: v1
kind: Pod
metadata:
  name: centos
spec:
  volumes:
  - name: shared-data
    emptyDir: {}
  containers:
  - name: centos
    image: centos
    command: ["/bin/sh","-c", "while true; do echo hello; sleep 10;done"]
    securityContext:
      privileged: true
